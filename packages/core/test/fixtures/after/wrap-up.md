---
sil:
  type: task
---
# Wrap up

Takes nothing from the earlier steps. Runs once at the end of the flow.

## Steps

Go through the documents in this repository and find descriptions that disagree with the code. Follow the [structure](structure.md#rollback) for the rollback procedure. Do not fix anything; report only `file:line` and a one-line reason.

## Seeded problem

The `#rollback` anchor does not exist in `structure.md`. `L-N09` catches it.
